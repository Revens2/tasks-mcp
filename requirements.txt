# Dépendances de la stack tasks (venv partagé upstream + passerelle).
# Versions alignées sur celles déjà validées sur cette machine (vault-mcp,
# calendar-mcp-gateway) : mcp 1.29.0, uvicorn 0.52.1.
mcp==1.29.0
uvicorn==0.52.1
httpx>=0.27,<1
icalendar>=6.0,<7
python-dateutil>=2.9,<3
pytest>=8,<9
