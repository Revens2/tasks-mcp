# Dépendances de la stack tasks (venv partagé upstream + passerelle).
# Migré SDK v2 le 2026-09-08 (FastMCP->MCPServer, suite verte, live 401/200/201).
mcp==2.2.0
uvicorn==0.52.1
httpx>=0.27,<1
icalendar>=6.0,<7
python-dateutil>=2.9,<3
pytest>=8,<9
